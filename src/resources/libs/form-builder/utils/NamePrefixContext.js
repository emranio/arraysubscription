import React from 'react';

// Create a context to hold the current name prefix for nested fields.
// This helps in constructing the full field name for rc-field-form.
export const NamePrefixContext = React.createContext([]);
